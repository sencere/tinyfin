/* Sigmoid shim: canonical implementation lives in src/ops_activation.c.
   This file intentionally contains no definitions to avoid duplicate
   symbol conflicts; it remains a compilation unit for historical builds. */
#include "ops_activation.h"
